# Dominios mobile

Los futuros dominios (`auth`, `profiles`, `bands`, `venues`, `providers`, `calls`, `events`,
`rehearsals`, `chat`, `marketplace`, `ratings`, `reports` y `admin`) se agregarán aquí como
rutas lazy y features verticales a medida que se implementen los siguientes paquetes de trabajo.
